// Google Fonts are loaded via <link> in _document.jsx to avoid build-time fetch.
// This object provides className and style for use throughout the app.
export const font = {
  className: "",
  style: {
    fontFamily: '"Roboto", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  },
};
